#!/bin/bash

MAIN_DIR=$(pwd)

for REPO in $STRINGLIST; do
    printf "\n-- $REPO\n";
    cd $MAIN_DIR/$REPO;
  if [ -z "$(git status --porcelain)" ]; then
      function_do_something_with_repo_without_changes
  else
      function_do_something_with_repo_with_changes
  fi
  cd $MAIN_DIR;
done
